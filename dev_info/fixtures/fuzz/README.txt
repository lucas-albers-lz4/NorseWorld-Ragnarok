Sequence fuzzer fixtures (#15)
==============================
Generated by: mono NWR.Tests.exe sequence-fuzz

Env overrides:
  NWR_FUZZ_SEED=15       master seed (default 15)
  NWR_FUZZ_N=1000        sequence count (default 1000)
  NWR_FUZZ_MIN_STEPS=3
  NWR_FUZZ_MAX_STEPS=8

Replay a failure: set NWR_FUZZ_SEED to the recorded per-sequence seed is not enough —
use the failure's "seed" field as RandomHelper/seq seed; easiest is re-run with the
same masterSeed and inspect the failure entry's steps string.

fuzz-report.json — pass/fail counts + up to 20 minimal failing sequences.
